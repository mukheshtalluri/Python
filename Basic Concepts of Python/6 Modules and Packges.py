"""
Modules : We can import custom created modules to use classes inside that modules.
Packages : Packages are the external library are external modules we can import and use for out requirement.
"""
# Packages : In python we have sum packages we can import and use as per our requirement.
import numpy as np
my_array = np.array([1, 2, 3, 4])
print(my_array)

